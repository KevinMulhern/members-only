<h1>Create a New Post</h1>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_for(@post) do |f| %>
			<%= f.text_area :body_text, class: 'form-control', rows: "6" %>

			<%= f.submit "Create Post", class: 'btn btn-primary' %>
		<% end %>
	</div>
</div>
